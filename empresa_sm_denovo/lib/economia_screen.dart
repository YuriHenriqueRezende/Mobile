import 'package:flutter/material.dart';

class EconomiaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Conteúdo relacionado à Economia',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}
