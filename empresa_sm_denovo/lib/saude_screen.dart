import 'package:flutter/material.dart';

class SaudeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Conteúdo relacionado à Saude',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}
