import 'package:flutter/material.dart';

class OhmsLawScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('1ª Lei de Ohm')),
      body: Center(
        child: Text('Esta é a tela de cálculo da 1ª Lei de Ohm'),
      ),
    );
  }
}
