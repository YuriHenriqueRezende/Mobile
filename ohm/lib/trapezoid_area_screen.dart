import 'package:flutter/material.dart';

class TrapezoidAreaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Área do Trapézio')),
      body: Center(
        child: Text('Esta é a tela de cálculo da área do trapézio'),
      ),
    );
  }
}
