import 'package:flutter/material.dart';

class TriangleAreaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Área do Triângulo')),
      body: Center(
        child: Text('Esta é a tela de cálculo da área do triângulo'),
      ),
    );
  }
}
